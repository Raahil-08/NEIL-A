{
    "name": "resiliwatch",
    "version": "1.0.0",
    "description": "ResiliWatch - 2-Service Cyber-Resilience Monitoring System",
    "private": true,
    "scripts": {
        "dev": "concurrently -n gateway,detector -c cyan,magenta \"npm run gateway\" \"npm run detector\"",
        "gateway": "cd gateway && node src/index.js",
        "detector": "cd detector && node src/index.js",
        "install:all": "npm install && cd gateway && npm install && cd ../detector && npm install",
        "start": "npm run dev"
    },
    "devDependencies": {
        "concurrently": "^8.2.2"
    }
}